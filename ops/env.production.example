# /home/ubuntu/new-wealth/api/.env — production. FOUR lines only; no secrets.
# All secrets come from AWS SSM /dhanam/newwealth/* (loaded at boot).
NODE_ENV=production
PORT=3020
SSM_PARAMETERS_PATH=/dhanam/newwealth/
SSM_REGION=ap-south-1
